{
  "name": "helix-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter \"*\" dev",
    "build": "bun --filter \"*\" build",
    "test": "bun --filter \"*\" test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
